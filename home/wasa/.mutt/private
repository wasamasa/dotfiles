# vim: ft=muttrc

  set from      = "hurrus.durrus@gmail.com"
  set sendmail  = "/usr/bin/msmtp -a private"
  set mbox      = "+private/archive"
unset record
  set postponed     = "+private/drafts"
  set realname      = "webspider"
  set signature     = ~/.mutt/private_sig

color status cyan default

macro index ,c "<change-folder>+public/inbox<return>" "change accounts"
macro index D "<save-message>+private/trash<enter>" "move message to the trash"
